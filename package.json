{
  "name": "preset",
  "version": "1.0.0",
  "description": "preset setup",
  "main": "ex1.js",
  "scripts": {
    "build": "babel src -d lib",
    "start": "npm run build && node lib/writeMentionableUsers.js",
    "test": "babel-node ./node_modules/.bin/_mocha $npm_package_options_mocha"
  },
  "options": {
    "mocha": "--require ./resources/mocha-bootload --check-leaks --full-trace --timeout 15000 src/tests/*.js"
  },
  "keywords": [
    "graphql"
  ],
  "author": "michael angerman",
  "license": "MIT",
  "dependencies": {
    "apollo-cache-inmemory": "^1.1.11",
    "apollo-client": "^2.2.7",
    "apollo-link-http": "^1.5.3",
    "graphql": "^0.13.2",
    "graphql-tag": "^2.8.0",
    "node-fetch": "^2.1.1",
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "babel-cli": "*",
    "babel-preset-env": "*",
    "chai": "4.1.2",
    "chai-subset": "1.6.0",
    "mocha": "5.0.0"
  }
}
